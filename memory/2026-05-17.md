# 2026-05-17

## Stock Analysis Pipeline Bug Fix

Darin caught 3 bugs with stock analysis pipeline:
1. **Wrong thread** — reports posted as their own threads instead of replies under his message
2. **Italic** — reports used `_italic_` throughout which rendered as italic on Slack
3. **Incomplete pipeline** — no acknowledgment, no progress updates, no GitHub push

**Fix applied:**
- Created `scripts/stock_analysis.py` — runs TradingAgents, outputs JSON progress lines, auto-formats for Slack (no italic), pushes to GitHub
- Updated TOOLS.md stock channel rules with exact protocol
- All Slack output now uses `threadId = source message ts` for correct threading
- All text filtered through `slack_text()` function (no `_italic_` remains)
- GitHub push verified with META analysis

**How to use:** Run `python3 scripts/stock_analysis.py <TICKER> <depth>` via exec with yieldMs for progress. Parse JSON progress lines, send Slack updates in the correct thread. Final output has github_url and slack_text.
