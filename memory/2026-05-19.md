# 2026-05-19

- Fixed xiaohongshu-mcp for US use by making base URL configurable and using rednote.com; confirmed QR login success.
- Installed autoclaw-cc/xiaohongshu-mcp-skills into workspace.
- Created/updated skills:
  - `xhs-mcp-workflow`: MCP health check + auto-restart cron (30m), login guard, XHS search workflow, ranking by likes+collects, Slack summary rules, language rules (ask user; default Chinese; don't translate names; keep XHS content original language), report format with **daily itinerary**, activities grouped by location, food mentions+sentiment, appendices (all posts + post/comment summaries), and structured JSON for reuse.
  - `xhs-trip-planner`: interview flow (MCQ prioritization + MCQ hotel-move preference + MCQ output language), checks existing XHS reports from past week, uses `xhs-mcp-workflow` for XHS research and `hotel-research` for hotels (no Airbnb), ADHD-friendly day-by-day output, progress updates every 2 min.
- Reports repo: `darinyu/deep-research-reports` exists (clone succeeded) and is the correct target for deep research / trading / XHS reports. Do NOT write reports to `darinyu/openclaw-config`.
- `push_xhs_report.py` updated to push reports into `deep-research-reports` under `xhs/<YYYY-MM-DD>/<keyword>/report.md`.
- Created Honolulu XHS report in new format (with appendices + JSON) and migrated other reports.
- Ran a skill audit (requested "ChatGPT 5.5"; closest available was GPT-5.2) and logged findings; fixed a medium bug where trip-planner Step 5 previously pushed to the wrong repo.

## Correction Retrospective (15:07 UTC)

Time window: 2026-05-18 15:07 UTC → 2026-05-19 15:07 UTC

### Scope
Scanned session transcripts, memory files, and Slack channels (#C0B4EK31NCU, DM, threads) for corrections from Darin.

### Corrections found: NONE

Darin's only interactions in the window:
- "retry" — asking to retry the Correction Retrospective cron (timed out)
- "update cronjob, mention what changed" — feature request for git sync
- "also add link to commit" — feature request for git sync
- "what files? what are the changes" — question about git sync contents
- "reply here" — routing instruction from another user

None of these were corrections about DDDD's behavior, output formatting, tool usage, or decisions.

### Previous fixes verified: ALL STILL IN PLACE ✅
1. Stock Analysis Pipeline (TOOLS.md, stock_analysis.py, slack_format.sh, push_trading_report.py)
2. Slack Routing rules (TOOLS.md, AGENTS.md)
3. Auto-log conversations (AGENTS.md, think.py)
4. Cron output rules (TOOLS.md)

## Retro item: Slack reply reflex during MCQ flow

Darin called me out for replying privately instead of Slack during the trip planner MCQ interview. My flow was:
1. ✅ React to message
2. ✅ Log to think.py
3. ❌ Reply PRIVATELY instead of Slack with `message(action=send)`

This happened MULTIPLE times in the same conversation. The AGENTS.md rule is clear — "The `visibleReplies: message_tool` setting means ONLY explicit message tool sends show up in channels." I need to retrain the reflex.

**Fix for next time:** After react + think.py log, the NEXT tool call MUST be `message(action=send)` — not a private session reply. If I'm in a Slack-originated thread, every response goes through the message tool, period.

## Slack Routing Issue (17:12-17:26 UTC)
- Darin @-mentioned DDDD in #random at 17:12 UTC asking to plan a Europe trip
- Follow-ups at 17:19 and 17:25 UTC also didn't trigger
- Finally triggered at 17:26 UTC with another @-mention
- Config: `requireMention: true`, `groupPolicy: "open"` — should have worked
- No session was ever created for the 17:12 message
- Root cause unknown — likely an intermittent Gateway issue
- TODO: Monitor if this happens again; check gateway logs next time

## Trip Planning: Italy + Greece 7-day (17:12-17:44 UTC)
- Darin requested 7-day Europe trip: Italy + Greece, summer, him+Paofu, moderate pace
- Final plan: Rome 4 days → Athens 3 days, 1 hotel each
- Language: Chinese, Priorities: highest-rated > foodie > Instagram
- Full report pushed to GitHub

## GitHub Data Path Structure (set by Darin, important!)
- **Raw data** → `/xhs/YYYY-MM-DD/<destination>/data.json` — consolidated JSON with all search results
- **Processed reports** → `/xhs-research/<destination>/` — 01_*.md, 02_*.md, 03_*.md, 04_*.md + xhs_search_results/*.json
- NEVER put processed reports in `/xhs/`
- NEVER put raw data.json in `/xhs-research/`
- Always push BOTH locations. Raw data first.
- Skill xhs-trip-planner was updated to enforce this.

## Standing rule: Always minimize hotel moves
- 1 hotel per city/destination. Never ask about this again.
- Skill xhs-trip-planner updated: removed the "Hotel moves" interview question, added standing rule.

## Initial response delay (17:12-17:26 UTC)
- Darin's @-mentions at 17:12, 17:19, 17:25 didn't trigger a session
- Only 17:26 triggered. Root cause unknown — likely intermittent Gateway issue.
- No config issue found (requireMention: true, groupPolicy: "open" — correct)

## Data.json must include desc + comments (corrected 17:51 UTC)
- Darin caught that raw data.json only had basic fields, no post details or comments
- Fixed: data.json schema now includes `desc` (full post text, up to 3000 chars) and `top_comments[]` (content, nickname, likes)
- Skill updated: mandatory enrichment step calls get_feed_detail for top 10 posts before pushing
- all_results[] must track `id` and `xsecToken` to support enrichment
- Existing honolulu data.json also lacks this — will be fixed next time it's touched

## Content-driven recommendations rule (set by Darin 21:49 UTC)
- Raw data.json must include `desc` (full post text) and `top_comments` (top 5 comments with content)
- `has_content` field marks whether the post has readable content
- **Hard rule:** Only posts with non-empty desc can be used for recommendations. No exceptions.
- Engagement metrics (likes, collects) are for ranking ONLY — the actual recommendation text comes from reading desc + comments and extracting sentiment, tips, warnings, specific mentions
- Skill updated: new ⚠️ warnings in Step 3, enrichment filtering, dat.json validation, recommendation format sections all updated

## Root cause: empty desc in data.json (21:53 UTC)
- `search_feeds` API only returns displayTitle + interactInfo (counts) — NO desc field
- `get_feed_detail` is the ONLY way to get post body text + comments
- Must call `get_feed_detail` SEPARATELY after search_feeds to enrich posts
- Skill enrichment section already updated to enforce this, with `has_content` tracking
- Italy+Greece: went from 3/15 posts with content → 8/15 after enrichment
