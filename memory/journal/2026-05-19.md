# Journal — 2026-05-19

## Infrastructure & Skills

- Fixed **xiaohongshu-mcp** for US use by making base URL configurable (rednote.com). Confirmed QR login success.
- Installed `autoclaw-cc/xiaohongshu-mcp-skills` into workspace.
- Created **`xhs-mcp-workflow`** skill: MCP health check + auto-restart cron (30m), login guard, XHS search workflow, ranking by likes/collects, Slack summary rules, language rules (ask user, default Chinese, don't translate names), report format with daily itinerary, appendices, and structured JSON.
- Created **`xhs-trip-planner`** skill: MCQ interview flow, checks existing XHS reports from past week, uses `xhs-mcp-workflow` + `hotel-research` for accommodation (no Airbnb), ADHD-friendly day-by-day output, progress updates every 2 min.
- Reports repo configured: `darinyu/deep-research-reports` is the single target for all reports. Updated `push_xhs_report.py` to push into `xhs/<YYYY-MM-DD>/<keyword>/report.md`.

## Data Structure Standards (set by Darin)

- **Raw data** → `/xhs/YYYY-MM-DD/<destination>/data.json` with consolidated JSON
- **Processed reports** → `/xhs-research/<destination>/` with numbered markdown files
- Never mix the two paths. Always push both.
- Raw data.json must include `desc` (full post text, up to 3000 chars) and `top_comments[]` (content, nickname, likes).
- Only posts with non-empty `desc` can drive recommendations — `search_feeds` doesn't return desc, `get_feed_detail` is the only source.
- Standing rule: **1 hotel per city/destination** — never ask about hotel moves.

## Trip Planning

- Darin requested a 7-day Europe trip: **Italy + Greece** (Rome 4 days → Athens 3 days) for him and Paofu, moderate pace.
- Language preference: **Chinese**
- Priorities: highest-rated > foodie > Instagram
- Full report pushed to GitHub.

## Corrections & Learnings

- **Correction Retrospective (15:07 UTC):** No new corrections from Darin in the 24h window. All previous fixes still in place.
- **Slack reply reflex bug:** During MCQ flow, I replied privately instead of via Slack `message(action=send)` multiple times. Retrained reflex: after react + think.py log, next call must be `message(action=send)`.
- **Slack routing issue (17:12-17:26 UTC):** Darin's @-mentions failed to trigger a session three times before finally working. Root cause unknown — likely intermittent Gateway issue.
